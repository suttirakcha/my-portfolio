export interface Certificates {
  title: string
  institute: string
  year: number
}

export interface ProjListProps {
  title: string
  desc: string
  projLink: string
  projCode: string
  img: string
}

export interface Menus {
  title: string
  id: string
}